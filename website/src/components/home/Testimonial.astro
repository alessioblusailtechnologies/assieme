---
import { testimonial as t } from '~/data/home';
---

<section class="quote section--alt" aria-labelledby="quote-title">
  <h2 class="visually-hidden" id="quote-title">La voce dei clienti</h2>

  <div class="quote__inner shell">
    <div class="quote__portrait hatch" role="img" aria-label="Ritratto del cliente — immagine in arrivo">
      <span class="mono" aria-hidden="true">ritratto cliente · 3:4</span>
    </div>

    <figure class="quote__figure">
      <blockquote class="serif quote__text">{t.quote}</blockquote>
      <figcaption class="quote__author">
        <span class="quote__name">{t.name}</span>
        <span>{t.role}</span>
        <span>{t.company}</span>
      </figcaption>
      <a class="btn btn--text" href={t.href}>
        Guarda la dimostrazione <span class="btn__arrow" aria-hidden="true">→</span>
      </a>
    </figure>
  </div>
</section>

<style>
  .quote {
    background: var(--c-page-alt);
    padding: var(--sp-section) var(--gutter);
  }

  .quote__inner {
    display: grid;
    grid-template-columns: 340px 1fr;
    gap: 5rem;
    align-items: start;
  }

  .quote__portrait {
    display: flex;
    align-items: flex-end;
    height: 400px;
    padding: 1rem;
    color: var(--c-text-4);
  }

  .quote__figure {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1.75rem;
    margin: 0;
    padding-top: 0.75rem;
  }

  .quote__text {
    margin: 0;
    max-width: 47.5rem;
    font-size: var(--t-h3);
    line-height: 1.34;
    letter-spacing: -0.012em;
    color: var(--c-text);
    text-wrap: pretty;
  }

  .quote__author {
    display: flex;
    flex-direction: column;
    gap: 2px;
    font-size: var(--t-sm);
    color: var(--c-text-2);
  }

  .quote__name {
    color: var(--c-text);
  }

  @media (max-width: 900px) {
    .quote__inner {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .quote__portrait {
      max-width: 340px;
      height: 300px;
    }
  }
</style>
