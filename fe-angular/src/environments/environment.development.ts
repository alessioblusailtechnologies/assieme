export const environment = {
  production: false,
  apiBase: '/api',
  devTools: true,
  /* In sviluppo l'API vera risponde dietro il proxy: la porta c'è anche qui. */
  accessoObbligatorio: true,
};
